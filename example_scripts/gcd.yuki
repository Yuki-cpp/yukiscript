(function gcd (a b)
(
	(function printl (x)
	(
		(print x)
		(print '\n')
	))
	
	(if (== a b)
	(
		(printl a)
		(set ret a)
	))
	(if (> a b)
	(
		(set ret (gcd ((- a b) b)))
	))
	(if (< a b)
	(
		(set ret (gcd (a (- b a))))
	))
))

(gcd (15 14))
